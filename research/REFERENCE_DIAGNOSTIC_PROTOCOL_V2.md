# Reference development diagnostic v2

Frozen before running this diagnostic, 12 September 2026 local time. Version 2 uses the repaired reference_env_v2 stack, recorded in reference-environment-v2-lock.txt. The original v1 GPU comparison never started because dependencies were blocked. The same preselected twelve cases and factorial design are retained. A separate identity_916 engineering example was inspected before this run; it is excluded from these twelve cases. The optional 1024 processing feature is excluded: all diagnostic candidates use 512. No website default will be chosen from this diagnostic alone. This is a small development diagnostic, not a final test or a novelty claim. Use all twelve cases from the signed reference_diagnostics_v1 manifest without selecting by generated quality. The manifest records selection failures and known limitations.

Run a 2 × 2 design: FaceID adapter scale 0 versus 0.8, and SDXL denoising strength 1.0 versus 0.99. Each uses the same four distinct references, exact eye-region mask, seed 17, 512 × 512 resolution, DDIM scheduler, nominal 30 steps, guidance 5, prompt, frozen weights and model offloading. Strength 0.99 executes 29 denoising steps rather than 30: this contrast does not isolate strength from step count. Scale zero retains the adapter graph with zero conditioning contribution. It isolates reference contribution within this implementation, not a separate stock-pipeline reproduction.

For every generated candidate, score both hard outside-mask composition and the established optional Poisson harmonization. Keep raw generation, hard and harmonized outputs. This yields 48 generated candidates and 96 scored rows. Clean target RGB and target landmarks must not be passed to inference; clean RGB is loaded by the evaluator only after generation for scoring. Validate all processed input hashes.

Primary diagnostic is whole-image AlexNet LPIPS. Also report hole RGB MAE, visible RGB MAE and MAE in the inner four-pixel hole boundary band, all on [0,1] RGB. Outside-mask pixels must equal the submitted processed input exactly for both compositors. Identity fidelity is not measured: no independent identity evaluator is installed for this diagnostic. Scores cannot establish correct recovery of hidden expression, identity or appearance.

Report all configurations and paired identity-level bootstrap 95% intervals for reference-on minus reference-off at matched strength/compositor, strength 0.99 minus 1.0, and Poisson minus hard. Use 10,000 bootstrap samples and seed 20260912. Twelve identity labels are the resampling units; do not treat 96 rows as independent people. Report every runtime failure and do not silently omit it from the denominator. Incomplete or failed runs cannot produce a completed comparison table.

The images are development-only, one synthetic eye occlusion per target, restricted to detector-success cases and existing CelebA identity labels. This does not establish robustness to inaccurate masks, poor references, new domains, full-face occlusion, real damage, or independent pretraining exposure. Do not silently update website defaults from this small diagnostic or call it a novel method.
